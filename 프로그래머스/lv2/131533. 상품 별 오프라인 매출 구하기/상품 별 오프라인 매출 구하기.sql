-- 코드를 입력하세요
SELECT P.PRODUCT_CODE, PRICE * S AS SALES FROM PRODUCT AS P INNER JOIN (SELECT PRODUCT_ID, SUM(SALES_AMOUNT) AS S FROM OFFLINE_SALE GROUP BY PRODUCT_ID) AS O ON P.PRODUCT_ID = O.PRODUCT_ID ORDER BY 2 DESC, 1 ASC;